defaults:
  - _self_
  - parameters_linear

hydra:
  run:
    # Simple output directory structure for linear elastic runs
    dir: results/${output_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: true